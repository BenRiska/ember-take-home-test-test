import { DepreciationTracker } from '@/components/DepreciationTracker';

export default function Home() {
  // abstracted in case we want to add a landing page later
  return <DepreciationTracker />;
}
